# Agent Rules

- Use Bun by default: `bun install`, `bun run <script>`, `bun test`, `bunx <pkg>`.
- Use TypeScript with Bun for quick scripts; avoid Python for one-off tasks.
- Keep changes scoped to the request; do not modify unrelated files.
- Do not run destructive git/history commands unless explicitly asked.
